# Taming the Stack (in PureScript)

*To view the presentation:*

```sh
python3 -m http.server
```

And navigate to `localhost:8000` in your browser.

*To run the tests:*

Install `spago` and `purs`.

```sh
spago test
```
